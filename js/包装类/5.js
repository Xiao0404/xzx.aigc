// var arr = [1,2,3,4,5]
// arr.length = 2
// console.log(arr);

var str = 'abcde' // new String('abcde)
str.length=2  // new String('abcde).length=2
// str.valueOf() 负责把一个包装类对象转为原始字面量
// 判断这个str不是对象，而是字符串字面量， delete str.length
console.log(str);